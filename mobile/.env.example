# Copy this file to .env and fill in real values before running the app.
# These are embedded into the client bundle at build time (Expo's
# EXPO_PUBLIC_* convention) — anyone who has the shipped app can extract
# them, same tradeoff the web app already makes with VITE_ANTHROPIC_KEY.
EXPO_PUBLIC_ANTHROPIC_KEY=sk-ant-your-key-here
